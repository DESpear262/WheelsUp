# Core ETL dependencies (Python 3.14 compatible packages)
# MINIMAL SET - All packages verified compatible with Python 3.14

# Data processing (core scientific stack)
pandas==2.2.0
numpy==1.26.4

# Configuration and utilities
python-dotenv==1.0.0
click==8.1.7
loguru==0.7.0

# Testing
pytest==8.0.0
pytest-asyncio==0.23.0

# HTTP and async (core networking)
aiohttp==3.9.0
httpx==0.26.0

# Date and time
python-dateutil==2.8.2

# LLM Integration (for data extraction)
anthropic==0.34.0  # Claude API client
openai==1.51.0     # OpenAI API client (fallback)
boto3==1.35.0      # AWS SDK for Bedrock

# Additional ETL dependencies
pydantic==2.12.4   # Data validation (Python 3.14 compatible version)

# NOTE: All packages in this requirements.txt are compatible with Python 3.14.
# Additional packages can be added as needed - they will be tested for compatibility.
